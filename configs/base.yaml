random_seed: 42
split:
  train_fraction: 0.70
  validation_fraction: 0.15
  test_fraction: 0.15
paths:
  raw: data/raw
  interim: data/interim
  processed: data/processed
  artifacts: artifacts
  metadata: data/metadata
